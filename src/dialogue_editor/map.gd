extends Node2D

# Declare member variables here. Examples:
# var a = 2
# var b = "text"

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.
	
var line_color = Color("ffffff")
var tile_size = 128
func _draw():
## UNFINISHED GRID DRAWING CODE 
#	draw_set_transform(Vector2(), 0, Vector2(tile_size,tile_size))
#	for y in range(0, tile_size):
#		draw_line(Vector2(0, y), Vector2(tile_size, y), line_color)
#	for x in range(0, tile_size):
#		draw_line(Vector2(x, 0), Vector2(x, tile_size), line_color)
# Called every frame. 'delta' is the elapsed time since the previous frame.
	pass